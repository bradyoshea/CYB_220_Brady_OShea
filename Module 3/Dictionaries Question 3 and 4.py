definition = {
    "python": "programming language",
    "variable": "stores values",
    "list": "stores multiple values",
    "method": "function that takes a class instance as a parameter",
    "if statement": "tests if a condition is true",
    "dictionary": "holds values tied to keys",
    "function": "block of code that is run when called"
}
for key, value in definition.items():
    print(f"{key.title()}: {value}\n")